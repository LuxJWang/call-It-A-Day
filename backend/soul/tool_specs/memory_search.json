{
  "name": "memory_search",
  "description": "Search for relevant memories from the user's diary and past conversations using semantic similarity search.",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "The search query to find relevant memories"
      },
      "limit": {
        "type": "integer",
        "description": "Maximum number of memories to retrieve (default: 5)",
        "default": 5
      }
    },
    "required": ["query"]
  },
  "returns": {
    "type": "array",
    "items": {
      "type": "object",
      "properties": {
        "id": {"type": "string"},
        "content": {"type": "string"},
        "metadata": {"type": "object"},
        "distance": {"type": "number"}
      }
    }
  },
  "examples": [
    {
      "query": "running injuries",
      "limit": 3
    }
  ]
}
